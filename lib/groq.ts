// Groq API client for the Coverstory excuse engine.
// Placeholder — the excuse-generation logic is added in a later layer.
// GROQ_API_KEY is server-only; never expose it to the client.

export const GROQ_API_KEY = process.env.GROQ_API_KEY;

export {};
